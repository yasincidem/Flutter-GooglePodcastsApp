import 'package:flutter/material.dart';

class SimpleListDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      color: const Color(0xFF3C4043),
      thickness: 1.0,
      height: 1.0,
    );
  }
}
